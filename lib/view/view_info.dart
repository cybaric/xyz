import 'package:flutter/material.dart';

class ViewInfo{

  ListView tabInfo() {
    return ListView(children: [
            Column(children: [
              ElevatedButton(onPressed: (){}, child: Text('a'))
            ],),
          ],);
  }
}